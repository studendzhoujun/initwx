var path = require('path');
module.exports = {
    entry: './index.js',
    output: {
        path: path.resolve(__dirname, 'bin'),
        filename: 'gome-utils-eventbus.js',
         // export itself to a global var
        libraryTarget: "umd",
        // name of the global var: "Foo"
        library: "gomeUtilsEventBus"
    },
    
    module: {
        loaders: [
            {
                test: /\.js$/,
                exclude: /(node_modules|bower_components)/,
                loader: 'babel-loader',
            },
        ]
    },
}